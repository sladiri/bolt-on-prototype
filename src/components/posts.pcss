.posts {
  & .posts__post {
    align-items: center;
    flex-wrap: wrap;
    padding: 1.5rem 1rem;

    & .posts__title {
      margin: 0;
      margin-right: 1rem;
    }

    & .posts__content {
      flex-basis: 100%;
      margin: 0;
      margin-top: 1.5rem;
    }
  }
}
